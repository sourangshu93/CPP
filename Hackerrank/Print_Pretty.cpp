#include <iostream>
#include <iomanip> 
#include <math.h>
using namespace std;

int main() {
	int T; cin >> T;
	cout << setiosflags(ios::uppercase);
	cout << setw(0xf) << internal;
	while(T--) {
		double A; cin >> A;
		double B; cin >> B;
		double C; cin >> C;
	int D=<floor(A)>
	hex


	}
	return 0;
}